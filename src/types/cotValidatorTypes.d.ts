export type SchemaFragment = {
  xsd: string;
  requiredEventAttributes: string[];
  requiredEventChildren: string[];
  requiredPointAttributes: string[];
};

// Add other type/interface declarations from cotValidator.ts here as needed.
